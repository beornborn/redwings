.row
  .col-sm-2
    = render 'left_navbar'
  .col-sm-6
    = render 'sync_buttons'
    br
    - if current_user.in_project? params[:filter]
      ul.list-group.users
        = render current_user
    ul.list-group.users
      = render @users

